name: CICD - ACR

on:
  schedule:
    - cron: "0 0 * * SUN"
  push:
    branches:
      - "**"
    tags:
      - "v*"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compliance:
    uses: VanOord/datalab-github-actions/cicd-acr.yml@master